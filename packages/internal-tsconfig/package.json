{
  "name": "@jitl/tsconfig",
  "private": true,
  "version": "0.29.2",
  "scripts": {
    "build": "yarn build:tsc && echo built $npm_package_json && ls -l",
    "build:tsc": "npx tsc --project tsconfig.tsup.json",
    "clean": "git clean -fx ."
  }
}
