{
  "name": "@jitl/tsconfig",
  "private": true,
  "version": "0.32.0",
  "scripts": {
    "build": "pnpm run build:tsc && echo built $npm_package_json && ls -l",
    "build:tsc": "npx tsc --project tsconfig.tsup.json",
    "clean": "git clean -fx ."
  }
}
